// 인간의 언어로 사용 의사 코드 Pseudo code 진짜와 비슷한 면도 있는데 진짜가 아니라는 뜻 수도코드 슈도 코드
// 언어와 상관없이 이해할 수 있음
// 알고리즘 볼 때 많이 봄
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	/*Initialize sum to 0
	* Prompt user (사용자에게 메세지를 전달해라 console창을 프롬프트라고도 한다.)
	* Read input
	* While 'the'(관사) input is 'an' integer 
	*	Add the input to sum, // while이라 tab을 함ㅋ
	*	Prompt user,
	*	Then read next input then을 써서 인간적으로 부드럽게 적음
	* After input complete, print sum*/
	
	//while (scanf("%d", &num) == 1) // while getting and testing the value succeeds 
	//{
	//	// do something with num //	Process the value 값을 가지고 뭔갈 한다는 걸 이렇게 씀 전산학과 같은 곳에서 많이 쓰이는 용어
	//}
	
	return 0;
}