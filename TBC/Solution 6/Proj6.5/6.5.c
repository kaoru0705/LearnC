/* True or false 사실과 거짓 */
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
//#define TRUE 1
//#define FALSE 0 요즘은 이렇게 안 씀 boolean type이 있다.
int main()
{
	/*int tv, fv;
	tv = (1 < 2);
	fv = (1 > 2);

	printf("True is %d\n", tv);
	printf("False is %d\n", fv);*/

	//int i = -5;
	//while (i)	// 0이면 false 그 외에는 true in c c++
	//	printf("%d is true\n", i++);
	//printf("%d is false\n", i);

	while (1)	//	많이 쓰는 용법
	{

	}

	return 0;
}