/* Variable & Constants */
#include <stdio.h>
// int angel = 1004(literal constant); // 말그대로(1004 그자체가 가진 이름)의 상수 
//const(Qualifier 한정자 제한자) const int angel(Symbolic constant 변수가 값을 바꿀 수 없는 기호적(변수의 이름이 있으니) 상수 값을 바꾸려하면 문법오류 컴파일이 막아버림)
int main() 
{
	printf("Hello World!");
	//p누르고 tab 하면 자동으로 쭉 문법이 적힘
	return 0;
}