#pragma once // header guard라 부름 표준은 아니다. 다음기회에

// #include <stdio.h> // 이게 없으면 오류 뜸 이젠 .c를 만들었고 이파일에는 필요없음

void print_hello();
void print_hi();
void print_str(char* str);

// assume that we have many more longer functions

